@page
@model KoCNihul.Pages.Diplomas.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>כל התעודות</h1>

<p>
    <a asp-page="Create">יצירת תעודה חדשה</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Diploma[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Diploma[0].Desc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Diploma[0].Context)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Diploma[0].Active)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Diploma) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Desc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Context)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Active)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DiplomaId">עריכה</a> |
                <a asp-page="./Details" asp-route-id="@item.DiplomaId">פרטים</a> |
                <a asp-page="./Delete" asp-route-id="@item.DiplomaId">מחיקה</a>
            </td>
        </tr>
}
    </tbody>
</table>
