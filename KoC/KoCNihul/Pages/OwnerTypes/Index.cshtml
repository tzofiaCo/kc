@page
@model KoCNihul.Pages.OwnerTypes.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>כל סוגי המפעילים/מפרסמים</h1>

<p>
    <a asp-page="Create">צור סוג מפעיל/מפרסם חדש</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OwnerType[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OwnerType[0].Desc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OwnerType[0].Context)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OwnerType[0].Active)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.OwnerType) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Desc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Context)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Active)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.OwnerTypeId">עריכה</a> |
                <a asp-page="./Details" asp-route-id="@item.OwnerTypeId">פרטים</a> |
                <a asp-page="./Delete" asp-route-id="@item.OwnerTypeId">מחיקה</a>
            </td>
        </tr>
}
    </tbody>
</table>
